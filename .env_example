# OpenAI API Key
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=your-api-key-here
# Hotkey to start/stop recording (hold to record, release to transcribe)
# Single keys: alt, ctrl, shift, f9, f10, space
# Combinations: alt+cmd, ctrl+shift+f9, alt+win, ctrl+alt+r
# Note: cmd/win/super are equivalent on different platforms
HOTKEY=alt

# Language for transcription
# Use 'auto' for automatic language detection (recommended for multilingual)
# Use 'en' for English, 'ru' for Russian, 'es' for Spanish, 'fr' for French, etc.
LANGUAGE=auto

# Transcription model to use
# Options: gpt-4o-transcribe, gpt-4o-mini-transcribe, whisper-1
TRANSCRIPTION_MODEL=whisper-1

# Maximum recording time in seconds
MAX_RECORDING_TIME=300

# Audio settings (optimized for macOS)
SAMPLE_RATE=44100
CHUNK_SIZE=1024

# Text processing settings
REMOVE_FILLER_WORDS=true
CUSTOM_FILLER_WORDS=basically,literally,actually

# Typing settings
# Lower values = faster typing, higher values = slower typing
TYPING_INTERVAL=0.01

# Typing method for multilingual support
# Options: auto (smart detection: direct for ASCII, clipboard for Unicode), clipboard, direct
TYPING_METHOD=auto

# Memory limit in MB (default: 100)
MAX_MEMORY_MB=100
